{
  "scripts": {
    "client": "cd client && npm install && npm start",
    "server": "cd server && npm install && nodemon --watch 'src/**/*.ts' --exec 'ts-node' index.ts",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "nodeman": "^1.1.2",
    "nodemon": "^3.0.1"
  }
}
