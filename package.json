{
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && nodemon --watch 'src/**/*.ts' --exec 'ts-node' index.ts",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
